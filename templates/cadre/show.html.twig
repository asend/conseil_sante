{% extends 'base.html.twig' %}

{% block title %}Cadre{% endblock %}

{% block body %}
    <h1>Cadre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cadre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cadre.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cadre_index') }}">back to list</a>

    <a href="{{ path('app_cadre_edit', {'id': cadre.id}) }}">edit</a>

    {{ include('cadre/_delete_form.html.twig') }}
{% endblock %}
