<form method="post" action="{{ path('app_cadre_delete', {'id': cadre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cadre.id) }}">
    <button class="btn">Delete</button>
</form>
