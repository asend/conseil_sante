<form method="post" action="{{ path('app_souhait_delete', {'id': souhait.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ souhait.id) }}">
    <button class="btn">Delete</button>
</form>
