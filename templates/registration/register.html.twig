{% extends 'baseauth.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="login-box">
        <div class="login-logo">
            <img src="{{ asset('build/images/cmslogo.jpg')}}" alt="logo" class="logo" >
        </div>
        <!-- /.login-logo -->
        <div class="card">
            <div class="card-body login-card-body">
                <p class="login-box-msg">INSCRIPTION</p>

                {{ form_start(registrationForm) }}
                    {{ form_row(registrationForm.email) }}
                    {{ form_row(registrationForm.plainPassword, {
                        label: 'Mot de passe'
                    }) }}
                    {{ form_row(registrationForm.nom_complet)}}
                    {{ form_row(registrationForm.agreeTerms) }}

                    <button type="submit" class="btn btn-primary btn-lg">S'inscrire</button>
                {{ form_end(registrationForm) }}

            </div>
        </div>
    </div>
    {# <form method="POST">
        <div class="input-group mb-3">
            <input type="email"  name="registration_form[email]" id="registration_form_email" class="form-control" autocomplete="email" required autofocus>
            <div class="input-group-append">
                <div class="input-group-text">
                    <span class="fas fa-envelope"></span>
                </div>
            </div>
        </div>
        <div class="input-group mb-3">
            <input type="password" name="registration_form[plainPassword]" id="registration_form_plainPassword" class="form-control" autocomplete="current-password" required>
            <div class="input-group-append">
                <div class="input-group-text">
                    <span class="fas fa-lock"></span>
                </div>
            </div>
        </div>
        <div class="input-group mb-3">
            <input type="email"  name="registration_form[nom_complet]" id="registration_form_nom_complet" class="form-control" autocomplete="email" required autofocus>
            <div class="input-group-append">
                <div class="input-group-text">
                    <span class="fas fa-envelope"></span>
                </div>
            </div>
        </div>
    </form> #}
{% endblock %}
