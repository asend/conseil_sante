<form method="post" action="{{ path('app_evasan_delete', {'id': evasan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evasan.id) }}">
    <button class="btn">Delete</button>
</form>
