{% extends 'base.html.twig' %}

{% block title %}Evasan{% endblock %}

{% block body %}
    <h1>Evasan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evasan.id }}</td>
            </tr>
            <tr>
                <th>Accompagnant</th>
                <td>{{ evasan.accompagnant }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ evasan.destination }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ evasan.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evasan_index') }}">back to list</a>

    <a href="{{ path('app_evasan_edit', {'id': evasan.id}) }}">edit</a>

    {{ include('evasan/_delete_form.html.twig') }}
{% endblock %}
