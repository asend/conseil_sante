{% extends 'base.html.twig' %}

{% block title %}Conseil index{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center">{{ 'listeConseil'|trans }}</h2>

        <div class="row">

            {% for conseil in conseils %}
                {{ include('conseil/_card.html.twig', {'patient': conseil}) }}
            {% else %}
                
            {% endfor %}
            
            {# <div class="col-md-3">2</div>
            <div class="col-md-3">3</div>
            <div class="col-md-3">4</div> #}
        </div>
    </div>

    {# <a href="{{ path('app_conseil_new') }}">Create new</a> #}
{% endblock %}
